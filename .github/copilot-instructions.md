This repository contains a Python SharePoint-only RAG teaching sample. Keep the main flow explicit: acquire a delegated token, make one Retrieval API POST, validate SharePoint results, then synthesize. Keep `dataSource` hardcoded to `sharePoint`, preserve site allowlist post-filtering, never send unvalidated results to the LLM, and avoid unnecessary wrappers or retry loops. Add or update tests with behavior changes.
